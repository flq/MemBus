<Window x:Class="Membus.WpfTwitterClient.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro" xmlns:UI="clr-namespace:Membus.WpfTwitterClient.Frame.UI" Title="MainWindow" Height="400" Width="1010" Background="{StaticResource ResourceKey=WindowBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1" />
            <RowDefinition />
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        
        <ContentControl Grid.Row="1" x:Name="ActiveItem" />
        
        <StatusBar Grid.Row="2" Style="{StaticResource ResourceKey=StatusbarStyle}" HorizontalAlignment="Stretch">
            <StatusBarItem HorizontalContentAlignment="Right">
                <StackPanel Orientation="Horizontal" Visibility="{Binding IsBusy, Converter={StaticResource ResourceKey=BoolToVisibility}, ConverterParameter=Collapsed}">
                    <Label Margin="5 0" x:Name="BusyMessage" />
                    <UI:GifImage Grid.Column="1" GifUri="pack://application:,,,/Resources/wait.gif" FramesPerSecond="5" DockPanel.Dock="Right" />
                </StackPanel>
            </StatusBarItem>            
        </StatusBar>
    </Grid>
</Window>
